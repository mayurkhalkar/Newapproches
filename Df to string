import pandas as pd

# Sample DataFrame
data = {
    'id': [1, 2, 3],
    'name': ['John', 'Jane', 'Mike'],
    'age': [30, 25, 35]
}
df = pd.DataFrame(data)

# Convert all columns to string data type
df_str = df.astype(str)

# Print the new DataFrame with all columns as strings
print(df_str)
